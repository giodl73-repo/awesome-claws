# Contribution records

Every Claw added after `contribution-policy.json` keeps one
`<claw-id>.json` record here. The record preserves the accepted proposal,
nearest alternatives, operational distinction, and proof plan.

Do not add post-policy Claw ids to the grandfathered list.
