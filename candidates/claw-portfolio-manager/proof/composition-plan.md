# Claw Portfolio Manager composition

**Decision:** IMPROVE/COMPOSE (standalone candidate rejected)
**Confidence:** 0.92
**Classification:** COMPOSE

- **repository-operations-manager** -> `portfolio-run-lineage`: Emit signed portfolio selection and continuation checkpoint fields.
- **repository-compliance-program-manager** -> `provider-issue-snapshot`: Emit complete provider issue snapshot receipts without invoking mutation.
- **contribution-admission** -> `typed-admission-decision`: Derive typed admission from a validated proposal and signed comparison.
- **catalog-quality** -> `signed-package-tree`: Publish a signed package-tree manifest over exact catalog and source bytes.
- **work-chief-of-staff** -> `stateless-budget-plan`: Plan against supplied capacity and external idempotency receipts without reserving.
- **composition-adapter** -> `externally-pinned-trust`: Pin public verification keys and reject unknown signer domains.
- **runtime-evidence** -> `minimized-usage-evidence`: Emit tenant-scoped minimized advisory outcome evidence.
- **work-chief-of-staff** -> `proposal-owner-handoff`: Render the combined evidence and proposal as an owner-controlled handoff.

No merge, publish, external mutation, atomic mutation, budget increase, risk
acceptance, HR inference, or sensitive-person inference authority is reachable.
